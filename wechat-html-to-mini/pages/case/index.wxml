<view class="page">
  <!-- States -->
  <view class="state-box" wx:if="{{loading}}"><text class="state-text">加载中...</text></view>
  <view class="state-box" wx:elif="{{error}}" bindtap="loadData"><text class="state-text">加载失败，点击重试</text></view>

  <block wx:if="{{!loading && !error}}">
    <!-- Page header with watermark -->
    <view class="page-header">
      <view class="wm-wrap">
        <text class="wm wm-ali">ALI LAOXI</text>
        <text class="wm wm-studio">STUDIO</text>
        <text class="wm wm-brow">bROW</text>
      </view>
      <text class="page-title">案例展示</text>
    </view>

    <!-- Type tabs -->
    <view class="tabs">
      <view class="tab-pill {{activeType === 'all' ? 'active' : ''}}" bindtap="onTypeChange" data-type="all">
        <text class="en">all</text>
        <text class="cn">全部</text>
      </view>
      <view class="tab-pill {{activeType === 'photo' ? 'active' : ''}}" bindtap="onTypeChange" data-type="photo">
        <text class="en">photo</text>
        <text class="cn">图片</text>
      </view>
      <view class="tab-pill {{activeType === 'video' ? 'active' : ''}}" bindtap="onTypeChange" data-type="video">
        <text class="en">video</text>
        <text class="cn">视频</text>
      </view>
    </view>

    <!-- Category filter -->
    <scroll-view scroll-x class="cat-bar" wx:if="{{categories.length > 1}}">
      <view class="cat-inner">
        <text class="cat-tag {{activeCat === item ? 'active' : ''}}"
          wx:for="{{categories}}" wx:key="*this"
          bindtap="onCatChange" data-cat="{{item}}">{{item}}</text>
      </view>
    </scroll-view>

    <!-- Case grid (双列) -->
    <view class="case-grid">
      <view class="case-card" wx:for="{{list}}" wx:key="id"
        bindtap="onCaseTap" data-id="{{item.id}}">
        <view class="card-thumb">
          <image src="{{item.cover || ''}}" mode="aspectFill" class="thumb-img" wx:if="{{item.cover}}"/>
          <view class="thumb-placeholder" wx:else/>
          <!-- 游客试看水印 -->
          <view class="trial-wm" wx:if="{{!isMember}}"/>
          <!-- 视频标记 -->
          <view class="badge-video" wx:if="{{item.type === 2}}">
            <image src="/static/svg/media-play.svg" class="play-icon-svg"/>
          </view>
          <text class="thumb-tag">{{item.category_name || '对比'}}</text>
        </view>
        <view class="card-info">
          <text class="card-name">{{item.title}}</text>
          <view class="card-action" catchtap="onToggleFav" data-id="{{item.id}}">
            <image src="/static/svg/action-heart.svg" class="action-svg {{liked[item.id] ? 'active' : ''}}"/>
          </view>
        </view>
      </view>

      <!-- Empty state -->
      <view class="empty-case" wx:if="{{!list.length}}">
        <text>暂无案例</text>
      </view>
    </view>

    <!-- Bottom spacer for tab bar -->
    <view style="height: 160rpx;"/>
  </block>
</view>
